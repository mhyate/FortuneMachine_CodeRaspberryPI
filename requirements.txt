# Dépendances communes (MacBook et Raspberry Pi)
pyserial==3.5
Pillow==11.1.0
PyYAML==6.0.2
requests==2.31.0
python-dotenv==1.0.1
tk==0.1.0

# Dépendances spécifiques à la Raspberry Pi
# Ces packages seront ignorés sur MacOS
adafruit-circuitpython-thermal-printer==1.4.0
python-escpos==3.1
python-barcode==0.15.1
qrcode==8.0
RPi.GPIO==0.7.1; sys_platform == 'linux'

# Dépendances de développement et tests
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
black==23.11.0
flake8==6.1.0
unittest-mock==1.3.0
coverage==7.3.2

# Dépendances système requises sur Raspberry Pi (à installer via apt)
# sudo apt-get install xprintidle
# sudo apt-get install python3-tk
# sudo apt-get install xserver-xorg

# Dépendances optionnelles pour la traduction
googletrans==3.1.0a0

# Dépendances pour les tests d'API
responses==0.24.1  # Pour mocker les requêtes HTTP
freezegun==1.4.0   # Pour les tests avec des dates fixes